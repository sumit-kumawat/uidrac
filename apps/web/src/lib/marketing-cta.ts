/** Shared equal-width marketing / header CTA button classes. */
export const headerCtaGroupClass = 'flex items-center gap-2 shrink-0';

/** Same width for Sign In + Get Started in the blue header. */
export const headerCtaBtnClass =
  'min-w-[9.25rem] w-[9.25rem] px-4 py-2 rounded text-sm font-semibold transition-colors whitespace-nowrap inline-flex items-center justify-center';

export const heroCtaGroupClass =
  'grid grid-cols-1 sm:grid-cols-2 gap-3 w-full max-w-lg mx-auto';

/** Same width for hero & bottom CTA button pairs. */
export const heroCtaBtnClass =
  'w-full px-6 py-3 text-sm font-semibold rounded transition-colors inline-flex items-center justify-center gap-2';
